# Password Generator

A simple Python program to generate secure random passwords using letters, numbers, and symbols.

## Features
- Generates strong passwords
- Custom password length
- Uses random and string modules

## How to Run
1. Run the Python file:
   python password_generator.py
2. Enter desired password length
3. Get generated password
